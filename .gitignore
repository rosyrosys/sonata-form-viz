# Audio
# We BUNDLE the Public Domain Alciatore K. 545 mvt. I recording as OGG
# (assets/mozart_k545_mvt1.ogg) so the tool works out of the box. Other
# commercial recordings supplied by the user remain ignored.
*.mp3
*.m4a
*.wav
*.flac

# Word artifacts left by Microsoft Word
~$*.docx
~$*.doc

# Random PDFs that may live next to the project
000002354153_*.pdf

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.tmp
*~

# Local Claude / preview server config
.claude/
_extract/

# Node (if anyone adds tooling later)
node_modules/
package-lock.json
